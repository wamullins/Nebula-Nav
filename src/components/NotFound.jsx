const NotFound = () => {
    return <div>Error 404. Collection not found.</div>

    //// this compoenent is useless,. only keeping it here for now to avoid annoying error messages. will be deleted tomorrow
}

export default NotFound